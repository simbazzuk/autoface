# AutoFace v0.30.8.4 — Neutral Homepage Demo Labels

The public homepage no longer uses realistic personal names for illustrative examples.

- Replaces demo/example names with neutral labels such as `Profile A`, `Profile B` and `Profile C`.
- Keeps real member naming behaviour unchanged inside authenticated AutoFace experiences.
- No Firebase, Discovery, recommendation, Atlas or profile-model changes.
